..to list processes using grep
ps aux | grep 'firefox'
.. or use 'top', to see dynamically changing list, 
top

to kill a process:
kill 6978
..if unsuccessful use:
kill -9 6978

CTRL+ALT+F2 -> to switch console, then you can kill those problematic processes
CTRL+ALT+F7 -> will bring you back to original console

..to list files with owners
ls -l 
ls -l document.txt

..in creating bash scripts e.g. myscript.sh, first line is always:
#!/bin/bash
# then comments here...
# then more comments here...

..to find a program we use 'which'
which bash

..to have execute permission to script use chmod 755
chmod 755 myscript.sh

-------------- ..implementing commandline arguments
$ cat morevariables.sh
#!/bin/bash
# A simple demonstration of variables
# Ryan 6/10/2015
 
echo My name is $0 and I have been given $# command line arguments
echo Here they are: $*
echo And the 2nd command line argument is $2

$ ./morevariables.sh bob fred sally
My name is morevariables.sh and I have been given 3 command line arguments
Here they are: bob fred sally
And the 2nd command line argument is fred
--------------

--------------..implementing back tick (`)
$ cat backticks.sh
#!/bin/bash
# A simple demonstration of using backticks
# Ryan 6/10/2015
 
lines=`cat $1 | wc -l` #this will have value of 12
echo The number of lines in the file $1 is $lines

$ ./backticks.sh testfile.txt
The number of lines in the file testfile.txt is 12
--------------

--------------..sample of backup script
$ cat projectbackup.sh
#!/bin/bash
# Backs up a single project directory
# Ryan 6/10/2015
 
date=`date +%F` #this displays when run in command-line: 2015-10-06
mkdir ~/projectbackups/$1_$date
cp -R ~/projects/$1 ~/projectbackups/$1_$date
echo Backup of $1 completed

$ ./projectbackup.sh ocelot
Backup of ocelot completed
--------------

read answer #asks input from user, like readline
if [ $answer != 'y' ]
then
    exit #program exits
fi #end of if-statement

...find files > 200MB in home dir
$ find /home -size +200M -exec ls -sh {} \;

..find all files in /etc directory modified in the last 24 hours.
find /etc -mtime -1

tar -zcvf mytar.tar.gz * #c - creates a tar.gz
tar -zxvf mytar.tar.gz   #x - extracts a tar.gz

'Awk' is a good program to use if you need to work with data that is organised into records with fields

'Scp' is a quick, easy and secure way to copy files between different machines.



